services:
  telegram-bot:
    build: .
    container_name: egofederation-telegram-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DB_PATH=/data/bot_data.sqlite3
    volumes:
      - bot_data:/data

volumes:
  bot_data:
